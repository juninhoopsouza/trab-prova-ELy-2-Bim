import 'package:flutter/material.dart';
import 'package:preferencias/listview_pessoa.dart';

void main() => runApp(
      MaterialApp(
        title: 'Returning Data',
        home: ListViewPessoa(),
      ),
    );
